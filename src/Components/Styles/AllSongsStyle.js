// @flow

import {StyleSheet} from 'react-native'
import {Colors, Fonts} from '../../Themes/Theme'

export const Styles = StyleSheet.create({
  container: {
    marginBottom: 230 // Для отображения блока с песней
  },
  title: {
    paddingTop: 10,
    paddingBottom: 10
  }
})